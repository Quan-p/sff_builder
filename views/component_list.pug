extends layout 

block content  
    h2= title 
    p This is a list of all the components.
    p 
    a(href='/component/create') Create Component
    ul
    each component in component_list
      li
        a(href=component.url) #{component.name}

    else
      li There are no components.