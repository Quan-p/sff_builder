extends layout 

block content  
    h1= title 
    p This is a list of all the categories
    p 
    a(href='/category/create') Create Category
    ul
    each category in category_list
      li
        a(href=category.url) #{category.title}

    else
      li There are no categories.